{
  "name": "background-replacer",
  "title": "Background Replacer",
  "main": "lib/middleware.coffee",
  "description": "Adds a background-color to every layer. So useful",
  "settings": {
    "replaceWithThisColor": {
      "type": "string",
      "default": "hotpink"
    },
    "beAnnoyingWithNotifications": {
      "type": "boolean",
      "default": true
    }
  },
  "enigines": {
    "csshat": ">2.0.0"
  },
  "scripts": {
    "test": "jasmine-node spec --coffee"
  },
  "dependencies": {
    "tinycolor2": "^1.0.0"
  }
}
